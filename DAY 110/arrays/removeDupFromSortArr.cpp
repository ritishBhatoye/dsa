#include<iostream>
using namespace std;

void removeDupFromSortArr(int *arr,int size)
{
    int i=0;
    for(int j=1;j<size;j++)
    {
        if(arr[i]!=arr[j])
        {
            
        }
    }
}

int main()
{
    int size = sizeof(arr) / sizeof(arr[0]);
    int arr[5]={1,1,2,2,3,3};
    return 0;
}